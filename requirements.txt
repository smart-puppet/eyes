# Prefer the system OpenCV from JetPack/apt when available.
opencv-python>=4.8.0; platform_machine != "aarch64"
numpy>=1.21,<2
# Optional: GPU floor/BEV. JetPack 6 / CUDA 12: pip install --user cupy-cuda12x
# tensorrt is provided by JetPack system packages (python3-libnvinfer)
